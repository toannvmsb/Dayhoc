# DạyZi — device test results (live)

Filled during real-device testing. `PASS` requires physical-device confirmation
from the tester. Severity: **P0** privacy/security/data-loss/crash · **P1** core
journey broken · **P2** major defect w/ workaround · **P3** polish.

## Environment

| | |
|---|---|
| Baseline commit | `299d41f` (SDK 57 + standalone dev APK) |
| Expo SDK | **57.0.0** (tester's Expo Go auto-updated 54→57; project upgraded to match — RN 0.86.3, React 19.2.3). Was 54, was 51. |
| App | **Standalone dev APK** `vn.dayzi.app.dev` (`npx expo run:android`, local Gradle build — machine has Android SDK at `D:/dev/android-sdk` + JDK 17 at `D:/dev/jdk17`). **Expo Go SDK 57 is abandoned** — it could not survive its own `ExperienceActivity` being recreated (logcat: destroy → recreate → reload fails with `MalformedURLException(null)` → frozen app; **D-22**). The dev APK handles the Android lifecycle correctly and still hot-reloads from Metro. Rebuild only when native config / deps change; JS changes are Fast Refresh. |
| Connection | **USB / `adb reverse`** (machine on corporate Wi-Fi `f88.corp` — client isolation blocks phone↔machine over LAN). `adb reverse tcp:8081 tcp:8081` + `tcp:3100 tcp:3100` (re-run after any phone reboot). adb at `/d/dev/android-sdk/platform-tools/adb`, device `RF8RC0ZJGEX` (SM-N980F, Android 13). `apps/mobile/.env` `EXPO_PUBLIC_DEV_HOST=localhost`. |
| API / web | `http://localhost:3100` (via adb reverse) — ready ✅, on `parent_copilot_pilot` |
| Metro | started by `expo run:android` (or `npx expo start --dev-client`), `apiBaseUrl http://localhost:3100/api/v1`. |
| DB | portable PG 16, **`parent_copilot_pilot`** — a dedicated DB for device testing (2026-09-03), 20 migrations. The test suite still uses `parent_copilot`; `pg-store.integration.test.ts` runs the legacy→identity backfill on whatever DB `DATABASE_URL` points at, so a `vitest run` was mutating the device-test seed. Launch the API with **`bash scripts/dev-pilot.sh`** (supervises `next dev` on the pilot DB, auto-restarts on crash — see D-17). |
| Seed | **Re-seeded clean 2026-09-03** (DB had ~13 duplicate `@dayzi.seed`/`@dayzi.local` users from repeated integration-test + partial-seed runs → non-deterministic dev-auth login; manually purged, then `seed:pilot`). Re-seeded 2026-09-03 (tester's app held a valid student session; reset to force login). `parent_copilot_pilot`: parent `phuhuynh@dayzi.seed`/`pilotpass1234`, teacher `giaovien@dayzi.seed`/`pilotpass1234`, seed student `hs-e4d80635@dayzi.local`/`hocsinh1234` (Bé Lớp 7), children *Bé Lớp 4* `92394c49-ad28-4920-8daa-a2798b70e11d` (has evidence/gap, **no** student login yet) / *Bé Lớp 7* `e4d80635-787d-4492-b2c8-10027f9435ea`. Roles verified parent/teacher/child + `[PARENT]`/`[TEACHER]`/`[STUDENT]`. |

---

## Android

| Device | OS | App build | Tester | Date |
|---|---|---|---|---|
| _(pending — tester to fill)_ | _(Android __)_ | Expo Go / dev (channel `dev`) | | 2026-09-02 |

| # | Test case | Result | Observed | Sev | Fix commit |
|---|---|---|---|---|---|
| 1 | App startup (open in Expo Go) | ✅ (w/ notes) | Welcome renders; "BẢN DEV" banner shows; form validation works; API reached from device (server returned the register error). Notes: (a) no custom blue splash — **P3**; (b) register endpoint error was English "email + 8-char password required" — **P2, fixed** → Vietnamese | P2 / P3 | `b047c1d` |
| 2 | Parent login (`phuhuynh@dayzi.seed`) | ✅ | Retest after D-04/D-05 fixes: login OK, lands on Parent Home, no error. (First pass found D-04 + D-05, both P1, both fixed & confirmed on device.) | P1 (fixed) | `791a1d0` / `e046149` |
| 3 | Parent Home (context = ESTIMATE, today plan) | ✅ | "Hôm nay dạy con gì?", "Bài đang học" card + today plan render for Bé Lớp 4. Sign-out returns to login. | — | — |
| 4 | Create / select Child (switcher, 2 children) | 🔄 | Switcher toggles Bé Lớp 4 ↔ Bé Lớp 7, content updates. Bé Lớp 7 has no input data yet → "đang học" empty (expected — seed only added evidence for Bé Lớp 4). *Create-child* still to test. | — | — |
| 5 | Practice (create → runner → submit → "Xong rồi!") | ✅ | Retest after fixes: runner opens, per-question ✓/✗, "Đáp án đúng" + input format hint all show. First pass found **D-06 P2** (no result) + **D-07 P1** (cold-server login crash) — both fixed & confirmed on device — and **D-08 P3** (`exact` answers need exact phrasing — mitigated by revealing the answer). Enhancement per tester: completion screen also shows "Con trả lời: …" next to "Đáp án đúng: …" for comparison (`a8c559c`). | P1 / P2 / P3 (fixed) | `a8c559c` / `62722e6` |
| 6 | Student access / login (create login, log in as student) | ✅ | Create student login for Bé Lớp 4 from the new mobile Settings card → email shown → sign out → student login → lands on student "Hôm nay" with the child's tasks. | P2 (D-09 fixed) | `62722e6` |
| 7 | Student practice (self-luyện → submit) | ✅ | 3rd retest — tester confirms OK. Runner opens & submits, "Hôm nay" tracks 1/1 → 2/2 → "làm xong rồi", "Bài tập" shows "Đã hoàn thành", "Tiến bộ" no error + has sign-out. Fixes: **D-11 P1** (asg_1 crash), **D-12 P1** (student got PARENT role), **D-13 P2** (409 race), **D-14 P3** (bare copy), **D-15 P2** (non-tracked tasks / no focus-refetch), **D-16 P2** (no student sign-out), **D-17 P2-infra** (dev server crash → supervisor). Server log: all 200s, 0×409, 0 crashes during the confirming run. | P1/P2/P3 (fixed) | `7f760fb` `8019efb` `9596b8d` `8f57c20` `aa21ab1` `037f5e6` |
| 8 | Parent Progress updates after student work | ✅ | Tester confirms everything renders as expected: Tiến độ shows the 3 axes + "Gần đây" + insight, Bài tập shows "Đã hoàn thành" with the 2 sessions, Hôm nay's "Cần chú ý" still coherent. Parent home/progress/practice refetch on focus. | — | `ab09201` |
| 9 | Teacher login (`giaovien@dayzi.seed`) | ✅ | Tester confirms: lands on "Học sinh", sees Bé Lớp 7, permissions shown, redeem-code card + sign-out present, twin/gaps empty (Bé Lớp 7 has no data — by design). | — | — |
| 10 | Parent ↔ Teacher connection (invite code → approve) | ✅ | Tester confirms full flow works on device (new **D-18** connect screen). Feedback → fixed **D-19**: invite code now has a "Sao chép mã" button (expo-clipboard) so the parent doesn't transcribe it. | P2 | `3e1e70a` |
| 11 | Permission grant / revoke (sensitive toggle reflects for teacher) | ✅ | Tester confirms: after granting the sensitive group, teacher's "Bé Lớp 4" shows Quyền / Mức độ nắm bài / Điểm cần củng cố, now with skill NAMES (D-19). | P2 | `3e1e70a` `0c22554` |
| 12 | School / Class (propose school, "Thêm lớp", gán con, privacy) | 🔄 | Bé Lớp 4 flow works on device. Bé Lớp 7 (already has SEED-7A1 PRIMARY): raw English error "child … already has an ACTIVE PRIMARY class enrollment" → **D-21 P2 fixed** (VN 409 + the screen now hides "Lớp chính" and defaults to "Lớp học thêm" when a primary exists). Also **D-22** reported: after switching Bé Lớp 4→7 the bottom nav taps froze until a full Expo reload (SDK 57 upgrade turbulence? — retest with a clean reload). | P2 | `4e9f313` |
| 13 | Camera (deny permission → grant → take photo → upload) | ⏳ | | | |
| 14 | Photo-library upload | ⏳ | | | |
| 15 | Evidence confirmation (untick wrong item, fix skill, confirm) | ⏳ | | | |
| 16 | Exam / Revision (create → revision map → result → diagnosis) | ⏳ | | | |
| 17 | Settings (plan change = no payment; privacy copy) | ⏳ | | | |
| 18 | Child deletion (request → cancel; re-request → wrong name → correct name) | ⏳ | | | |
| 19 | Network / error states (airplane mode, expired session, 429) | ⏳ | | | |

Legend: ⏳ not started · 🔄 in progress · ✅ pass · ❌ fail

---

## iOS

_(prepared after Android is complete)_

---

## Defect log

| ID | Sev | Test | Description | Status |
|---|---|---|---|---|
| D-01 | P2 | 1 | `/api/v1/auth/*` returned English technical errors ("email + 8-char password required", "no account with this email") to end users | **fixed** — Vietnamese messages in `rest.ts`; verified via LAN |
| D-02 | P3 | 1 | No custom blue splash screen on native (SDK 54 moved `splash` config to the `expo-splash-screen` plugin; top-level `splash` key ignored) | open — polish batch |
| D-04 | P1 | 2 | Parent home showed "not a guardian of this child" on first login: the persisted `dz.child.v1` selection from a previously-registered throwaway account was reused without checking it belongs to the signed-in parent | **fixed** — `child.tsx` gains `reconcile(availableIds)`; `parent/home.tsx` calls it against `/children` and never uses an unknown id as `activeId`; `auth.tsx` clears `dz.child.v1` on sign-out. Mobile typecheck + 636 tests green |
| D-06 | P2 | 5 | Practice completion screen showed only "Xong rồi!" — the submit response (`results[].correct` / `verificationLevel`) was discarded, so the parent got no sense of how the child did | **fixed** — `run/[assignmentId].tsx` keeps the submit result and shows "Con làm đúng X/Y câu" for deterministically-verified items + "N câu cần bố mẹ xem lại" for `AI_CROSSCHECK_REQUIRED` (never labels those correct/incorrect — M46). Typecheck green; API shape verified by LAN curl |
| D-05 | P1 | 2 | Sign-out from Settings cleared the session but did not leave the screen — `app/index.tsx` only gates the initial launch, nothing watched the session on deep screens | **fixed** — `_layout.tsx` adds an `AuthGate` effect: when the session becomes null anywhere (sign-out or global 401), `router.replace('/welcome')`. Mobile typecheck + 636 tests green |
| D-07 | P1 | 5 | On a freshly-started API process, the first request being `/auth/login` or `/auth/register` failed: `rest.ts` `db()` read `globalThis.__dzPool` directly and assumed some other path (`/ready`, a prior request) had already created the pool | **fixed** — `api.ts` exports `pool()`; `rest.ts` `db()` calls it so the shared pool is created lazily on first use. Cold-path login verified over LAN; 636 tests green |
| D-09 | P2 | 6 | Mobile parent app had no screen to create / view / revoke a child's student login — the `/children/:id/student-access` routes exist and the web has the UI, but mobile Settings did not | **fixed** — "Tài khoản cho con" card added to `apps/mobile/app/parent/settings.tsx` (create w/ password ≥8 → shows login email; revoke; re-create after revoke). All 3 routes verified over LAN. Mobile typecheck clean |
| D-11 | P1 | 7 | Student "Hôm nay" tasks link to ephemeral plan ids (`asg_1`, `asg_2` — built in-memory by `childToday`, never persisted); tapping one opened `/run/asg_1` → `getAssignment('asg_1')` → Postgres `invalid input for type uuid`. Student could not start any assigned work | **fixed** — `apps/mobile/app/student/today.tsx`: tapping a task now materialises a real practice session via `POST /children/:id/practice` (like the student Practice screen) and opens that. 636 tests green |
| D-11b | P3 | 7 | The uuid cast error surfaced to the client as a raw PG message (`invalid input for type uuid: "asg_1"`) — a 500 leaking internals, against hardening §9 | **fixed** — `rest.ts` `uuid()` guard on `/assignments/:id` + `/assignments/:id/submit` → friendly 404 "Không tìm thấy bài tập." |
| D-12 | P1 | 7 | Every account created via `IdentityService.register` — students included — got legacy `users.role = 'parent'` (hardcoded in `PgIdentityStore.insertUser`). The idempotent legacy→identity backfill (`backfillIdentityFromLegacy`, run by `pg-store.integration.test.ts` against the same portable Postgres used for device testing) then granted `PARENT` in `user_roles`. Result: signing in as a student routed to the parent workspace and showed "Thêm con của bạn" | **fixed** — `insertUser(user, legacyRole?)`; `register` passes `STUDENT → 'child'` (backfill skips `'child'`). Verified: new student accounts have `users.role='child'`, `user_roles=[STUDENT]`; 636 tests green. Seed DB re-created clean |
| D-13 | P2 | 7 | The child today/progress/review screens each call `refreshLearningState`; right after new evidence they all see the snapshot cache stale and race to persist it → the loser hits a unique violation (`23505`), surfaced as a 409 on the student Progress screen (self-heals on pull-to-refresh) | **fixed** — the persisted snapshots/skill-states/gaps/plan are a cache and the computed scene is always correct, so the write block swallows `23505` and returns. Verified: 9 concurrent post-submit reads, 0×23505 |
| D-14 | P3 | 7 | After a child finishes the day's work the plan becomes `no_plan_needed`; the student "Hôm nay" then showed a flat "không có bài bắt buộc. Nghỉ ngơi nhé!" and the previously-listed optional "Thử thách" disappeared with no explanation | **fixed** — `buildChildToday` now uses the plan's own child-appropriate `reason` ("phần đang học đã ổn… có thể cho con làm 1 bài thử thách tư duy…"); the mobile empty state offers a "Luyện thêm 15 phút" button |
| D-17 | P2 (infra) | 7 | Submitting the 2nd task showed "Không có kết nối mạng" on every screen; other sites worked fine. Cause: the long-running `next dev` API server process died (clean-ish exit, no stack — likely OOM / webpack-worker crash after ~1h on this monorepo; ~13 orphan node processes had also accumulated across restarts/reboots). The mobile client's 20s timeout surfaces as `OfflineError` | **mitigated** — `scripts/dev-pilot.sh` supervises `next dev` (2GB heap, auto-relaunch in 2s on exit) so a crash costs a tester seconds, not the session. Not a product bug: resumed the exact crashed submit over LAN, `done 2/2` correct |
| D-22 | P1 | 12 | Reproducible after SDK 57: navigating in/out of a sub-screen (Trường & lớp) then pressing back froze the app — touches gave press feedback but nav/buttons did nothing; only the native header back worked. Persisted through several fix attempts | **root cause (logcat): Expo Go SDK 57 cannot recover when Android recreates its `ExperienceActivity`** — `onHostPause`/`onHostDestroy` → new Activity → reload fails with `java.net.MalformedURLException` (null URL) → frozen. **Not our code.** **Resolved by moving off Expo Go to a standalone dev APK** (`expo run:android`, `299d41f`) — tester confirms the APK is stable. Also shipped along the way: `cd1bd5b` (a real refetch-loop from `useFocusEffect` callback churn → `useReloadOnFocus`), `4d3141e` (`router.replace`→`navigate` for tabs), `83a63b0` (`enableFreeze(false)`) — all kept as hygiene |
| D-21 | P2 | 12 | Enrolling Bé Lớp 7 into a class leaked a raw English error ("child … already has an ACTIVE PRIMARY class enrollment"); the screen also let you pick "Lớp chính" when one already exists | **fixed** — `rest.ts` maps that + the dup-school-enrollment error to a Vietnamese 409; `school.tsx` drops "Lớp chính" from the options and defaults to "Lớp học thêm" when the child already has an ACTIVE PRIMARY |
| D-20 | P2 | 12 | The mobile parent app had no UI for the school/class enrollment capability (web has `/be/[childId]/truong-lop`). The production API had every method but `rest.ts` exposed none | **fixed** — 8 routes in `rest.ts` (schools search/propose, academic-years, classes list/propose, school/class enrollment, class-enrollment privacy PATCH — mobile client gains `patch()`); `listEnrollments` DTO resolves `schoolName`/`className`; `app/parent/school.tsx` linked from Settings: current school + per-class 3-way privacy picker, search→pick/propose school→enroll→list/add class→assign with type+privacy |
| D-19 | P2 | 11 | Teacher child-detail listed raw skill IDs (`M4.FRAC.COMMON_DENOM`) under "Mức độ nắm bài" / "Điểm cần củng cố"; and the invite code had no copy button | **fixed** — `teacherGetTwinSummary`/`teacherGetGaps` DTOs add `skillName` (kb lookup); mobile renders it + a VN gap-lifecycle label; `parent/connect.tsx` gets a "Sao chép mã" button (expo-clipboard `~8.0.8`) |
| D-18 | P2 | 10 | The mobile parent app had no UI for the relationship/permission capability — no way to generate an invite code, approve a teacher's request, or set what a teacher sees (web has `/be/[childId]/ket-noi`). Test 10/11 blocked | **fixed** — `apps/mobile/app/parent/connect.tsx` (linked from Settings → "Giáo viên của con"): pending requests accept/reject, connected-teacher list with a per-teacher grouped permission editor (incl. the "nhạy cảm — mặc định tắt" twin/gaps group), invite-code generator. Backend routes were already present; full flow verified over LAN |
| D-16 | P2 | 7 | A student login had no way to sign out — student nav is today/practice/progress and none carried a sign-out (`WorkspaceSwitcher` hides itself for a single-workspace user). Tester got stuck in the student app | **fixed** — "Đăng xuất" card on the student Progress screen; `AuthGate` (D-05) then routes to `/welcome` |
| D-15 | P2 | 7 | Student "Hôm nay" `tasks` came from an ephemeral plan preview and `completedAssignmentIds` was hardcoded `[]`, so `doneCount` was always 0 and finished tasks never left the list. Student screens also used `useQuery(…, [])` and never refetched on focus, so returning from the runner showed stale data | **fixed** — `studentGetToday` builds `tasks`/`doneCount`/`totalCount` from the child's real assignments created today; `student/{today,practice,progress}.tsx` refetch via `useFocusEffect`; today taps a real task to open it (empty state starts a new practice); practice screen shows a "Đã hoàn thành" section |
| D-10 | P3 | 6 | `scripts/seed-pilot.mjs` `wipe()` matches only `%@dayzi.seed` and never deletes the `hs-*@dayzi.local` student **user** rows it creates, nor dedupes — repeated runs accumulate orphan users that make dev-auth login (`SELECT … WHERE primary_email = … LIMIT 1`) non-deterministic | open — extend `wipe()` to also purge `@dayzi.local` users + guard against duplicate-email users |
| D-08 | P3 | 5 | Some seed questions use `answerSpec.kind: "exact"` with multi-part prose answers (`"8/12 và 9/12"`); a parent who does the maths correctly but phrases it differently is marked wrong with no explanation | **mitigated** (content limitation, not a bug) — completion screen now shows `Đáp án đúng: …` for wrong deterministic items and the runner shows a per-kind input hint. Full fix (looser `exact` matching / convert to `fraction`+`numeric` items) deferred — out of device-test scope |
| D-03 | P0 (infra) | 2 | Device saw stale English + `ENV_REQUIRED` errors — cause: `next build` was run while `next dev` was live on port 3100, corrupting `apps/web/.next` (`Cannot find module './chunks/vendor-chunks/next.js'`). The dev server then served production-mode / stale code | **fixed** — killed 3100, `rm -rf apps/web/.next`, restarted `next dev` clean; auth verified over LAN. **Rule: never run `next build` while `next dev` uses the same `.next`.** |
