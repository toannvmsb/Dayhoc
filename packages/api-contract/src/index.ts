/**
 * @copilot/api-contract — the shapes the API returns, shared by web + mobile.
 * Parent views carry the full picture; Child views are a server-built safe
 * projection with the sensitive fields absent, not merely hidden.
 */
export * from './view-models.js';
