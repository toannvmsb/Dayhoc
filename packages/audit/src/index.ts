/**
 * @copilot/audit — recommendation → evidence trace (Phase 10, DoD MVP:
 * "mọi khuyến nghị truy vết được"). Every gap, prescription and plan action can
 * be resolved to the deterministic rule that produced it and the raw evidence
 * records that justify it, with any AI inference ids surfaced explicitly.
 */
export * from './trace.js';
