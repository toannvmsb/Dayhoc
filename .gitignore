# Dependencies
node_modules/
.pnp.*
.yarn/

# Build output
dist/
build/
.next/
out/
*.tsbuildinfo

# Test / coverage
coverage/
.vitest/
test-results/
playwright-report/
blob-report/
.playwright/

# Local dev upload store (M4 — bytes live outside git and Postgres)
.dz-uploads/
**/.dz-uploads/

# Env & secrets (NEVER commit)
.env
.env.*
!.env.example
*.pem
*.key

# Logs
logs/
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Expo / RN
.expo/
**/.expo/
apps/mobile/expo-env.d.ts
*.orig.*

# Large copyrighted reference material (SGK textbook PDFs — kept locally, not versioned)
Tai lieu/

# Expo export output
apps/mobile/dist/

# Document-vision benchmark (P4) — real homework/test photos of real
# children. Never enters git history. expected/ + manifest.json (no image
# bytes) ARE committed — see packages/testing/vision-benchmark-data/README.md.
packages/testing/vision-benchmark-data/images/

# anh's original real homework photos (source for the copies above) — same
# reason, never enters git history.
File du an/BaiTap4/
CALIB*.txt
ROUND*.txt
ROUND*.jsonl
WS_E2E*.txt
WS_E2E*.jsonl
INSP.txt
PICK.txt

WS_SMOKE*.txt
WS_SMOKE*.jsonl
